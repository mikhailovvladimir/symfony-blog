{% extends 'base.html.twig' %}

{% block body %}
    <p>{{ post.body }}</p><a href="{{ path('blog_post_edit', {'slug': post.slug}) }}">Редактировать</a>
    <a href="{{ path('blog_post_delete', {'slug': post.slug}) }}">Удалить</a>
{% endblock %}